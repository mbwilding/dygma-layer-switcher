[workspace]
resolver = "2"
members = [
    "dygma-layer-switcher",
    "dygma-layer-switcher-gui",
    "common",
    "windows",
]

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
