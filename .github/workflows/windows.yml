---
name: Windows

on: [push]

jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@master
      - name: Compile
        run: cargo build --release
      - name: Show Directory Tree
        run: tree /F /A
